/* eslint-disable */
/*!
 */

export default function SaladMixer(account, web3) {
  
  var _web3 = web3;
  var ethAddress = account.address;
  var salad = {
    testMode: false,
    network: "auto",
  }

  return salad;
};